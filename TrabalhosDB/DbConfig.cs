namespace TrabalhosDB
{
    public static class DbConfig
    {
        // Atenção: senha deixada vazia para não subir no GitHub, a senha esta informada no arquivo enviado pelo teams.
        public static string ConnectionString = "Host=localhost;Username=postgres;Password=;Database=TrabalhosDB";
    }
}
